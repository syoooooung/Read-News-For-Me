services:
  - type: web
    name: read-news-for-me
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn news_analyzer.api:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
